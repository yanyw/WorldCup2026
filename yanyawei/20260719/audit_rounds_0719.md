# 2026-07-19 两轮审核记录

分析代理先按取消分歧惩罚后的规则生成模型、快照和初版结论；独立审核代理只读核算，分析代理可接受或拒绝意见，不以迎合为目标。

## 第 1 轮

### 审核通过项

1. `p_trade` 中已不存在内部/外部分歧惩罚，`model_disagreement` 只作诊断输出。
2. 1X2 逐家去水、机构平均、Opta 融合与 Yes/No 互补均无算术错误。
3. 计划仓位远小于第一档深度，VWAP 等于 ask；当前市场 `rate=0.05`、`exponent=1`，费用计算正确。
4. 模型截止日为 7 月 19 日之前，决赛结果没有泄漏；合约按前 90 分钟加补时结算。
5. 空仓成立。阿根廷胜 Yes 与小 2.5 的 taker edge 分别为 -0.7126pp 和 -0.7127pp。

### 审核意见与分析代理处理

1. **政策文本称经过 model uncertainty，但未校准 P10 没有进入交易层。**
   - 接受文本问题。
   - 拒绝把未经回测校准的 `scenario_p10` 直接塞入 `p_trade`。
   - 返修：全局政策改为只扣“适用且已验证”的项，并明确 P10/P90 仅作诊断。
2. **快照缺少本地可复核的结算规则和 CLOB 费用参数。**
   - 接受。
   - 返修：新增 `description`、`resolution_source`、`condition_id`、`fees_enabled` 以及 CLOB `fd`、tick、最小数量和基础费用字段。
3. **低于 best bid 的 maker 限价可以合法挂出，不应笼统写成不可执行。**
   - 接受其分类意见，但不把低成交概率条件单称为当前下注。
   - 返修：区分 `POST_ONLY_ACTIONABLE / TOP_OF_BOOK_ACTIONABLE` 与 `CONDITIONAL_LIMIT_BELOW_BID`；后者不进入正式仓位。
4. **继承文本含 Azteca altitude 和 two event market sets。**
   - 接受并清理为决赛语境和单事件描述。
5. **O/U 只有一条完整直接报价，建议补第二家。**
   - 作为可选增强保留。本轮对 totals 增加额外数据质量项，不伪造来源；该不足不会把负 edge 变成下注。

### 返修后重算

Polymarket 在 20:07:59 北京时间重新刷新，核心价格未变：

- 阿根廷胜 Yes：26.50 / 26.75c。
- 小 2.5：58.75 / 59.00c。
- Gamma 与 CLOB 对四个直接市场的费用参数均为 `rate=0.05`、`exponent=1`、`takerOnly=true`。

返修只改善证据与状态表达，没有改变概率中心、`p_trade` 或正式仓位。

## 第 2 轮

审核代理重新读取返修后的脚本和数据，确认：

1. 所有内外及模型组件分歧均不会扩大交易不确定性、降低 `p_trade` 或自动触发证据门。
2. 未校准情景区间仅输出，不参与 `u`。
3. 四个直接市场均保存了 90 分钟结算文字、FIFA 结算来源、费用开关与 CLOB 费用详情。
4. 8 个候选的费用手算误差为零。
5. `CONDITIONAL_LIMIT_BELOW_BID` 不进入 `selected_positions`。
6. 最新输出 `selected_positions=[]`，没有 taker 或当前最优买价 maker 达到 1.5pp。

第 2 轮结论：**通过，可以定稿。20:07:59 快照下正式空仓。**

## 最终独立复核表

| 候选 | `p_trade` | taker 成本 | taker edge | 当前 bid 处 edge | 条件限价 |
|---|---:|---:|---:|---:|---:|
| 阿根廷胜 Yes | 27.0171% | 27.7297% | -0.7126pp | +0.5171pp | 25.50c |
| 小 2.5 | 59.4968% | 60.2095% | -0.7127pp | +0.7468pp | 57.75c |

条件限价只是合法但低于当前 best bid 的观察单价，不是正式建议仓位。临场若触价，必须重新抓取首发、机构赔率、费用和订单簿。
